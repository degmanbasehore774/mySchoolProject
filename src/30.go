package main

import "fmt"

func main() {
    // Example of simple Go code with comments
    x := 5
    y := 10
    if x < y {
        fmt.Printf("x is less than y\n")
    } else {
        fmt.Printf("x is not less than y\n")
    }
}
